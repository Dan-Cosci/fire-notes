import { db } from '../../config/firebase.js';
import {  } from 'firebase/firestore';


const NoteService = {
  getNotes : async () => {},
  createNote: async () => {},
  updateNote: async () => {},
  deleteNote: async () => {},
  getNoteById: async () => {}
}

export default NoteService;